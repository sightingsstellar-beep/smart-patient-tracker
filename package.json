{
  "name": "glide-bedside",
  "version": "1.2.3",
  "description": "Glide Bedside — fluid intake/output, wellness checks, and daily reports via Telegram bot and web dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --check server.js && node --check db.js && node --check bot.js && node --check scripts/verify-clerk-credentials.js && node --check scripts/migrate-sqlite-to-postgres.js"
  },
  "dependencies": {
    "@clerk/express": "^2.1.15",
    "@phosphor-icons/web": "^2.1.2",
    "better-sqlite3": "^12.6.2",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-session": "^1.19.0",
    "multer": "^2.0.2",
    "node-cron": "^3.0.3",
    "node-telegram-bot-api": "^0.66.0",
    "nodemailer": "^6.10.1",
    "openai": "^4.28.0",
    "pg": "^8.20.0"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "license": "MIT"
}
